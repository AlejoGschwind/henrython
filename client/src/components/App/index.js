const { default: Button } = require("../Button");

function App() {
  return (
    <div className="App">
      <Button>
        Enviar
      </Button>
      <Button>
        Login !!
      </Button>
    </div>
  );
}

export default App;
